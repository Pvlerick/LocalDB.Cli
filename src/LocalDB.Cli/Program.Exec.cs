using CommandLine;
using LocalDB.Cli.Options;

namespace LocalDB.Cli
{
    partial class Program
    {
        static int Exec(string name, string command, string script)
        {
            return 0;
        }
    }
}
