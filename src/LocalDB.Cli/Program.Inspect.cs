using CommandLine;
using LocalDB.Cli.Options;

namespace LocalDB.Cli
{
    partial class Program
    {
        static int Inspect(string name, bool machineReadableFormat)
        {
            return 0;
        }
    }
}
